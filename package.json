{
  "name": "runbook-vscode",
  "displayName": "Runbook",
  "description": "VS Code bridge for the Runbook control surface (Claude Code agent loop)",
  "version": "0.1.0",
  "publisher": "runbook-rs",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "Runbook",
      "properties": {
        "runbook.daemonUrl": {
          "type": "string",
          "default": "ws://127.0.0.1:29381/ws",
          "description": "WebSocket URL for the Runbook daemon."
        },
        "runbook.escWhenPending": {
          "type": "string",
          "default": "cancel_only",
          "enum": ["cancel_only", "cancel_and_passthrough"],
          "enumDescriptions": [
            "Esc clears the pending prompt without sending Esc to the terminal (safe default).",
            "Esc clears the pending prompt AND sends Esc to the terminal."
          ],
          "description": "Controls Esc behavior when a prompt is pending (loaded). 'cancel_only' swallows Esc locally; 'cancel_and_passthrough' also forwards Esc to Claude."
        }
      }
    },
    "commands": [
      {
        "command": "runbook.connect",
        "title": "Runbook: Connect to daemon"
      },
      {
        "command": "runbook.disconnect",
        "title": "Runbook: Disconnect from daemon"
      },
      {
        "command": "runbook.dispatchTest",
        "title": "Runbook: Dispatch test prompt"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package",
    "pretest": "npm run compile",
    "test": "node ./dist/test/runTest.js",
    "test:bdd": "npm run compile && node ./dist/test/runBdd.js"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.6.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.18.1",
    "@vscode/test-electron": "^2.5.2",
    "glob": "^13.0.6",
    "mocha": "^11.7.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "ws": "^8.17.0"
  }
}
